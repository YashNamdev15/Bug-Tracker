version: "3.8"

services:

  backend:
    build: ./Backend
    container_name: bugtracker-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./Frontend
    container_name: bugtracker-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend